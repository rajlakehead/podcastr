//https://legible-teal-52.clerk.accounts.dev

const authConfig = {
    providers: [
      {
        domain: "https://legible-teal-52.clerk.accounts.dev",
        applicationID: "convex",
      },
    ]
  };

export default authConfig;